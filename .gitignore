# Chat sessions are stored in browser localStorage — not in the repo.
# Workspaces live at ~/ollama-chat-workspaces/ — not in the repo.
# SQLite retrieval indexes live inside each workspace dir — not tracked here.

# Old singular workspace dir (pre multi-workspace refactor)
workspace/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
